import {AsciiOnlyDirective} from './ascii-only.directive';

describe('AsciiOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AsciiOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
