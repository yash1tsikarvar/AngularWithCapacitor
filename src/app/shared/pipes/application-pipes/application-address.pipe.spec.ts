import {ApplicationAddressPipe} from './application-address.pipe';

describe('ApplicationAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
