import { CoApplicantTypePipe } from './co-applicant-type.pipe';

describe('CoApplicantTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CoApplicantTypePipe();
    expect(pipe).toBeTruthy();
  });
});
