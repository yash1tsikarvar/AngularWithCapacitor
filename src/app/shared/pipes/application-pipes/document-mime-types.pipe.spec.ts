import {DocumentMimeTypesPipe} from './document-mime-types.pipe';

describe('DocumentMimeTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentMimeTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
