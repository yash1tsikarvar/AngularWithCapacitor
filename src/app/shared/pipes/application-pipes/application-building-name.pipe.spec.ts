import {ApplicationBuildingNamePipe} from './application-building-name.pipe';

describe('ApplicationBuildingNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationBuildingNamePipe();
    expect(pipe).toBeTruthy();
  });
});
