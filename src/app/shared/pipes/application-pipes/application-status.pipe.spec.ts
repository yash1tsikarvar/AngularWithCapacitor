import {ApplicationStatusPipe} from './application-status.pipe';

describe('ApplicationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
