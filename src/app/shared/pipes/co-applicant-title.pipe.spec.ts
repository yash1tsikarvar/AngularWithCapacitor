import { CoApplicantTitlePipe } from './co-applicant-title.pipe';

describe('CoApplicantTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CoApplicantTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
