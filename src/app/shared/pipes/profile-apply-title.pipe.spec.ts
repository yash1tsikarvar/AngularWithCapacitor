import {ProfileApplyTitlePipe} from './profile-apply-title.pipe';

describe('ProfileApplyTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileApplyTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
