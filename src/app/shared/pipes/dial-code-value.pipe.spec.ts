import { DialCodeValuePipe } from './dial-code-value.pipe';

describe('DialCodeValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DialCodeValuePipe();
    expect(pipe).toBeTruthy();
  });
});
