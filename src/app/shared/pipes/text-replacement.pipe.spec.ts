import {TextReplacementPipe} from './text-replacement.pipe';

describe('TextReplacementPipe', () => {
  it('create an instance', () => {
    const pipe = new TextReplacementPipe();
    expect(pipe).toBeTruthy();
  });
});
