import { PropertyNamePipe } from './property-name.pipe';

describe('PropertyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
