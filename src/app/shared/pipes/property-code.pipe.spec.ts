import { PropertyCodePipe } from './property-code.pipe';

describe('PropertyCodePipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyCodePipe();
    expect(pipe).toBeTruthy();
  });
});
