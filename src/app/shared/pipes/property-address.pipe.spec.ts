import { PropertyAddressPipe } from './property-address.pipe';

describe('PropertyAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
