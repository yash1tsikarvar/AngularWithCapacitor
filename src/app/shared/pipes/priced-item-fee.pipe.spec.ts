import { PricedItemFeePipe } from './priced-item-fee.pipe';

describe('PricedItemFeePipe', () => {
  it('create an instance', () => {
    const pipe = new PricedItemFeePipe();
    expect(pipe).toBeTruthy();
  });
});
