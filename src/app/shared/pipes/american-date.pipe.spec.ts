import {AmericanDatePipe} from './american-date.pipe';

describe('AmericanDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AmericanDatePipe();
    expect(pipe).toBeTruthy();
  });
});
